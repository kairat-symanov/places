<nav class="navbar fixed-top navbar-light bg-primary navbar-expand-md">
  <div class="container">
        <button type="button" class="navbar-toggler collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            &#x2630;
        </button>
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav"></ul>
      <div class="navbar-nav ml-auto">
          <div class="navbar-nav ml-auto">
            <% if user_signed_in? %>
            <div class="nav-item">
              <span class="navbar-text">
                Hello, &nbsp;
              <span>
            </div>
            <div class="dropdown show">
                <%= link_to current_user.name, edit_user_registration_path(current_user), 
                class: 'btn btn-primary dropdown-toggle', 'role' => "button", 'id' => "dropdownMenuLink", 
                'data-toggle' => "dropdown", 'aria-haspopup' => "true", 'aria-expanded' => "false" %>
                  <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                      <%= link_to 'Редактировать профиль', edit_user_registration_path(current_user), class: 'dropdown-item' %>
                      <%= link_to 'Выйти', destroy_user_session_path, method: :delete, class: 'dropdown-item' %>
                  </div>
                    <div>
            <% else %>
                  <div class="nav-item">
                      <%= link_to 'Войти', new_user_session_path, class: 'nav-link' %>
                  </div>
            <% end %>
            </div>
          </div>
      </div>
  </div>
</nav>
